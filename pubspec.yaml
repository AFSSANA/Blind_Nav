name: blind_nav
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_tts: ^4.2.3         # 🔊 For voice output
  geolocator: ^14.0.2         # 🗺️ For GPS location access
  geocoding: ^2.0.5          # to get exact location(not saying longitude and latitude)
        

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  